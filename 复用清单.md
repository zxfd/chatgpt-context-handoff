# 复用清单

## 直接复用

- ChatGPT/Codex 原生插件清单：`.codex-plugin/plugin.json`。
- 原生生命周期钩子：`hooks/hooks.json` 中的 `Stop`。
- 原生续发语义：`decision: "block"` 与 `reason`。
- 原生循环标志：`stop_hook_active`。
- 原生会话信息：`session_id`、`cwd`、`transcript_path`。
- 原生插件目录：`PLUGIN_ROOT`、`PLUGIN_DATA`。
- Ponytail 的薄适配原则：小型命令钩子、插件数据目录、失败放行、无第三方依赖。
- Node.js 标准库：JSON、文件尾读取、SHA-256、原子标记文件和测试。

## 明确不做

- 不注入 ChatGPT 网页，不使用 Chrome 扩展或 DOM 选择器。
- 不调用 OpenAI API，不要求 API Key，也不重复支付一套模型请求。
- 不自建 tokenizer；优先采用客户端已经记录的 token 遥测。
- 不让钩子直接控制桌面 UI；自动新任务只调用客户端向模型开放的原生任务工具。
- 不依赖 Ponytail 运行时代码；参考其插件模式，避免把无关功能一起引入。
