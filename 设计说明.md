# 设计说明

## 目标

这是一个只面向 ChatGPT 桌面客户端中 Codex 工作区的原生插件，不是浏览器扩展。

当一次任务在 `Stop` 阶段达到配置的上下文阈值时，钩子返回 `decision: "block"`。客户端会把钩子的 `reason` 作为一条新的用户提示继续当前任务，让模型再执行一轮并写出 `交接文档.md`。

## 数据来源

钩子只读取客户端传入的 `transcript_path`，从最近的 `token_count` 事件中取：

- `last_token_usage.input_tokens`
- `model_context_window`

`transcript_path` 的格式不是稳定接口，因此解析器只识别上述窄结构；文件缺失、格式变化、配置错误或状态目录不可写时全部放行，不阻塞正常任务。

## 触发规则

默认配置：

- 绝对阈值：250,000 输入 token。
- 上下文窗口保护线：85%。
- 实际阈值：两者中的较小值。

默认值会在当前客户端约 258,400 token 的窗口上于约 219,640 token 触发，也会在 GPT-5.6 的 272,000 长上下文加价线之前留出交接余量。

用户可以通过 `~/.codex/context-handoff.json` 或环境变量覆盖这些值。

## 防循环

1. `stop_hook_active: true` 时立即放行，避免交接轮再次触发 Stop 续发。
2. 首次触发会在 `PLUGIN_DATA` 下按 `session_id` 写入一个标记，同一任务只交接一次。
3. 新任务有新的 `session_id`，无需清理旧标记即可重新监控。

## 新任务模式

- `manual`（默认）：交接完成后提示用户在 ChatGPT 客户端新建任务。
- `auto`：交接提示要求模型在客户端提供原生 `create_thread` 能力时创建并打开新任务；能力不可用时自动退回手动方式。

钩子本身不模拟点击，也不操作网页。
